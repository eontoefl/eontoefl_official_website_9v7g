-- =====================================================================
-- 실전 리포트: 질문 순서 (관리자 드래그 정렬)
--
-- 관리자가 진행 중 질문을 핸들바로 드래그해 순서를 바꾸면 여기 저장되고,
-- 학생 설문도 이 순서로 노출된다. 값이 없는 질문은 등록순으로 뒤에 붙는다.
-- =====================================================================

ALTER TABLE toefl_survey_questions
    ADD COLUMN IF NOT EXISTS sort_order int;
