-- =====================================================================
-- 실전 리포트: 휴대폰 번호 직접 수집
--
-- 기프티콘 받을 번호를 설문에서 직접 받는다 (혜택 프레임이라 마찰 낮음).
-- 회원 DB 이름 매칭의 실패 모드(오타·동명이인·세션 꼬임)를 제거하고,
-- 엑셀 다운로드는 응답에 적힌 번호를 그대로 쓴다.
-- (번호가 없는 옛 응답은 기존 회원 DB 매칭으로 fallback)
-- =====================================================================

ALTER TABLE toefl_survey_responses
    ADD COLUMN IF NOT EXISTS user_phone text;
