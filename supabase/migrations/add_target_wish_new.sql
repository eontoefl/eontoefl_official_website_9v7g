-- =====================================================================
-- applications.target_wish_new  (희망점수 = 2차 목표)
--
-- 커트라인(target_cutoff_new)은 관리자가 정하는 1차 목표(넘어야 할 선).
-- 희망점수는 커트라인을 넘긴 학생이 스스로 정하는 2차 목표(가고 싶은 선).
--   - 학생이 마이페이지에서 직접 입력한다 (후기성 가치가 없어 카톡/관리자로 묶지 않음).
--   - 커트라인 달성 후에만 입력이 열리고, 커트라인보다 높은 값만 유효하다.
--   - 신규 척도(1.0~6.0, 0.5 단위) 기준.
-- =====================================================================

ALTER TABLE applications
    ADD COLUMN IF NOT EXISTS target_wish_new numeric;
