-- =====================================================================
-- 실전 리포트: 기프티콘 발송 완료 표시
--
-- GiftyShowBiz 발송 결과 파일을 관리자 화면에 드롭하면
-- 매칭된 응답 행에 발송 시각이 기록된다.
-- 발송 완료된 응답자는 다음 기프티콘 엑셀 다운로드에서 자동 제외.
-- =====================================================================

ALTER TABLE toefl_survey_responses
    ADD COLUMN IF NOT EXISTS gifty_sent_at timestamptz;
