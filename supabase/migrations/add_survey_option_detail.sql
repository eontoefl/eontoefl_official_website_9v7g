-- =====================================================================
-- 실전 리포트: 객관식 보기 조건부 서술칸
--
-- 변경점
--   1) 응답 테이블에 서술 답변 컬럼(answer_detail) 추가.
--      - 학생이 "기타/아니오"처럼 서술칸이 딸린 보기를 고르면 여기에 저장.
--      - 기존 answer(고른 보기 라벨)는 그대로 → 집계·중복검사 로직 무변경.
--   2) 보기(options) 형식 확장 — 스키마 변경 없음(jsonb라 유연).
--      - 기존: 문자열 배열            ["2문단", "3문단"]
--      - 확장: 서술칸 보기는 객체로   ["3문단", {"label":"기타","detail":true,"ph":"직접 적어주세요"}]
--      - 코드가 문자열/객체 둘 다 읽으므로 기존 문항은 그대로 동작(하위호환).
-- =====================================================================

ALTER TABLE toefl_survey_responses
    ADD COLUMN IF NOT EXISTS answer_detail text;
