-- =====================================================================
-- 실전 리포트: 질문 보이기/숨기기 토글
--
-- 진행 중 질문을 마감하지 않고 잠시 학생에게 안 보이게 하는 용도.
-- (수정은 지원하지 않음 — 응답과 어긋나므로. 바꾸려면 삭제 후 재등록)
-- =====================================================================

ALTER TABLE toefl_survey_questions
    ADD COLUMN IF NOT EXISTS hidden boolean NOT NULL DEFAULT false;
