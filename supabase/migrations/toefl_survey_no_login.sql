-- =====================================================================
-- 실전 리포트: 로그인 없이 제출 허용
--
-- 카톡 인앱 브라우저의 로그인 마찰로 응답이 새는 것을 막기 위해
-- 로그인 대신 이름 입력으로 전환. 기프티콘은 관리자가 명단 검수 후
-- 수동 발송이므로 신원 게이트는 사람이 담당한다.
-- 로그인된 상태로 제출하면 user_id는 그대로 붙는다(자동 채움).
-- =====================================================================

ALTER TABLE toefl_survey_responses
    ALTER COLUMN user_id DROP NOT NULL;
