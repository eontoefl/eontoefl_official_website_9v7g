# 돈관리 작업 규칙

- 돈 기록은 사용자의 직접 실행으로만 바꾼다.
- 예외는 사용자가 2026-08-30에 직접 승인한 `main` → `main-v4` 최초 exact 이행 한 번뿐이다. 정확한 기록 번호·방향·목적별 금액이 모두 맞을 때만 실행하고 `distributionMigrationVersion=1`을 남긴다. 이 값이 있으면 거래 정정을 다시 실행하지 않는다.
- 기존 수입을 배분할 때 새 수입을 만들지 않는다.
- 배분은 원래 입금과 정확한 기록 번호로 연결한다.
- 이름, 날짜, 금액만 보고 과거 기록을 일괄 정정하지 않는다.
- 자료를 정정하기 전에는 원본을 백업하고, 정정 뒤에는 다시 조회해 확인한다.
- 암호화된 한 덩어리 저장과 이 기기 저장 기능을 유지한다.
- 온라인 동기화 여부를 실제 상태와 다르게 표시하지 않는다.
- 남편계좌는 급여가 들어오는 자산 계좌가 아니라 대표의 돈으로 매달 고정비를 내는 지출 통로다.
- 남편계좌의 거래 잔액을 순자산이나 다음 달 이월액으로 사용하지 않는다.
- 남편계좌의 월 확보 상태는 그 달 전체 입금과 전체 출금으로 계산한다.
- 코드 폴더의 기존 미추적 파일과 이번 작업과 무관한 변경은 건드리지 않는다.
